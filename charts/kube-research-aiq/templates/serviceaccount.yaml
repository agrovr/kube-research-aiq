{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kube-research-aiq.serviceAccountName" . }}
  labels:
    {{- include "kube-research-aiq.labels" . | nindent 4 }}
{{- end }}
